package general

/**
 * Object of all 4 directions (LEFT, RIGHT, UP, DOWN).
 * Used to avoid typos in string parameters.
 */
object Direction {
  val LEFT = "LEFT"
  val RIGHT = "RIGHT"
  val UP = "UP"
  val DOWN = "DOWN"
}
